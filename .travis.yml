sudo: required

language: sh

services:
  - docker

env:
  - SERVICE=ash
  - SERVICE=bash
  - SERVICE=checkbashisms
  - SERVICE=mksh
  - SERVICE=shellcheck
  - SERVICE=zsh

script:
  - docker-compose run $SERVICE

jobs:
  include:
    - script: docker-compose run kcov
              && bash <(curl -s https://codecov.io/bash)
      env:
        - SERVICE=kcov
